spring:
    datasource:
        url: jdbc:mysql://${ip}:3306/mms?useSSL=false&useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&rewriteBatchedStatements=true&allowPublicKeyRetrieval=true
        driver-class-name: com.mysql.cj.jdbc.Driver
        # com.mysql.cj.jdbc.Driver 有时候上面会失效， 下面这个可行
        username: root
        password: CHANGE_ME_DB_PASSWORD
ip: localhost
